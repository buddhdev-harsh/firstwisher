language: python

python: 
  - "3.6"

services: 
  - docker

install: 
  - pip install -r requirements.txt

after_install:
  - docker build -t harshdevl/wisher:latest .
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push harshdevl/wisher:latest
